listener 1883 0.0.0.0

# Spec §4.6 — IoT devices authenticate via passwd file.
# allow_anonymous true keeps python-ml MQTT subscriber working without creds in compose.
allow_anonymous true
password_file /mosquitto/config/passwd
